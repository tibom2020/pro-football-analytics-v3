[build]
# Copy data + History vào server/ — Railway root=server không có ../data
buildCommand = "cp -r data server/data && cp -r History server/History && cd server && npm ci && npm run build"

[deploy]
startCommand = "cd server && npm start"
restartPolicyType = "ON_FAILURE"
