# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
server/.venv/
.venv/
dist
dist-ssr
*.local
server/.env
.env

# Vite / frontend — không commit file chứa secret (chỉ `.env` ở trên không bao trùm `.env.local`)
.env.local
.env.*.local

# Runtime exports (live session — không commit)
History_live/
server/data/

# Local / tooling
.claude/
vite.config.ts.timestamp-*.mjs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
